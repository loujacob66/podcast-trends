#!/usr/bin/env python3
import sys
import os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))

import typer
from commands import top, stats

app = typer.Typer(help="📊 Analyze podcast trends by feature, date, and downloads.")

app.command(name="top")(top.top_episodes)
app.command(name="stats")(stats.show_stats)

if __name__ == "__main__":
    app()
